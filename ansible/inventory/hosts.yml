# Portfolio Application Inventory
# Configured for local WSL deployment

all:
  hosts:
    portfolio-server:
      ansible_host: localhost
      ansible_connection: local
      ansible_python_interpreter: /usr/bin/python3

  vars:
    # Deploy locally using Ansible on WSL
    ansible_connection: local
